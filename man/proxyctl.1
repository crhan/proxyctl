.TH PROXYCTL 1 "2026-05" "proxyctl 0.2.2" "User Commands"
.SH NAME
proxyctl \- Proxy configuration lifecycle management for macOS / Linux
.SH SYNOPSIS
.B proxyctl
[\fB--json\fR] [\fB--no-color\fR] [\fB--quiet\fR] \fICOMMAND\fR [\fIARGS...\fR]
.SH DESCRIPTION
.B proxyctl
是一套面向 macOS（含 Linux 部分支持）的代理引擎生命周期管理工具，
管 mihomo / sing-box 的启停、状态、健康检查、DNS 防护、配置切换。
.PP
它不负责安装代理引擎、不改具体规则、不改订阅——这些去 mihomo 自身的配置文件里改。
.PP
\fBAI Agent 友好\fR：所有读类命令支持 \fB--json\fR；
\fBPROXYCTL_AGENT=1\fR 一键开启 JSON + 关色 + 非交互；
错误信息一律带 \fIhint\fR + \fIdoc\fR；退出码分语义；写操作用文件锁。
.SH GLOBAL OPTIONS
.TP
.BR --json
所有读类命令输出 envelope JSON（schema v1）。\fBbench --json\fR 是 NDJSON 流式 + 末尾 summary。
.TP
.BR --no-color
关闭 ANSI 颜色（默认按 isatty / NO_COLOR / TERM=dumb / PROXYCTL_NO_COLOR 自动判断）。
.TP
.BR --quiet ", " -q
抑制非必要输出。
.TP
.BR --help ", " -h
显示帮助。子命令也支持：\fBproxyctl <cmd> --help\fR。
.TP
.BR --version ", " -v
显示版本。
.SH COMMANDS
.SS 自描述（Agent 入口）
.TP
.B proxyctl agent-guide
输出给 LLM 的入门 markdown（能力边界 / 退出码 / JSON envelope / 故障决策树）。
.TP
\fBproxyctl explain\fR [\fITOPIC\fR]
无参输出"想改 X 去哪？"速查表；带 topic 输出卡片。Topic 见下方。
.TP
.B proxyctl commands [--json]
输出所有命令的元数据：side_effects / needs_sudo / interactive / exit_codes / examples。
.TP
\fBproxyctl config\fR \fIpath\fR | \fIget\fR <key> | \fIset\fR <key> <value>
查询 / 修改 proxyctl 自身配置。支持 dot 路径（\fBcorp_dns.server\fR）。set 是原子写 + .bak 备份 + YAML 校验 + 失败回滚。
.TP
.B proxyctl doctor [--json]
极简 5 项健康打分：engine_up / port_listen / dns_ok / system_proxy_ok / connectivity_ok。
.TP
\fBproxyctl completion\fR \fIbash\fR | \fIzsh\fR | \fIfish\fR
生成 shell 补全脚本。

.SS 生命周期
.TP
.B proxyctl start / stop / restart / restart-clean
启停代理引擎（写系统 DNS / 代理 / launchd）。
.TP
.B proxyctl fix
修复 DNS / 代理 / 热重载。

.SS 诊断
.TP
.B proxyctl status [--json]
系统状态面板（引擎 / 端口 / TUN / DNS / 系统代理 / 网络环境 / 插件 sections）。
.TP
.B proxyctl check [--json]
4 阶段健康检查（基础状态 / 代理组 / 连通性 / 出口 IP）。
.TP
\fBproxyctl trace\fR \fIDOMAIN\fR \fB[--json]\fR
域名链路诊断（DNS / 规则预测 / 连通性 / 实际连接）。
.TP
.B proxyctl audit [DAYS|apply] [--json]
扫描代理日志找疑似应直连的域名；\fBapply\fR 写入 rules 段。
.TP
.B proxyctl bench [GROUPS...] [--json]
代理组测速；\fB--json\fR 是 NDJSON 流式（每节点一行）+ 末尾 envelope summary。

.SS 配置
.TP
.B proxyctl mode [tun|proxy]
切换流量入站方式（写 mihomo 配置）。
.TP
.B proxyctl engine [mihomo|singbox]
切换后端实现。
.TP
.B proxyctl daemon <NAME> [start|stop|restart|status|log]
管理 extra_daemons（如 claude-proxy）。

.SS DNS / 工具
.TP
.B proxyctl dns-lock / dns-unlock
启动 / 停止 DNS 看门狗。
.TP
.B proxyctl env [--unset]
输出代理环境变量（可 \fBeval $(proxyctl env)\fR）。
.TP
.B proxyctl log [--tail N] [--no-follow] [--json]
查看后端日志。默认 \fBtail -f\fR；\fB--no-follow\fR 立即返回。
.TP
.B proxyctl plugins [--json]
列出已加载插件。
.TP
.B proxyctl recover
切网后软恢复（清 DNS 缓存 + 重测代理组，不重启进程）。
.SH EXPLAIN TOPICS
\fBrules\fR, \fBnodes\fR, \fBconfig\fR, \fBdns\fR, \fBengine\fR, \fBports\fR,
\fBenv\fR, \fBcorp-dns\fR, \fBextra-daemons\fR, \fBplugins\fR,
\fBtroubleshooting\fR, \fBexit-codes\fR, \fBagent\fR.
.SH JSON ENVELOPE (schema v1)
.nf
{
  "schema_version": 1,
  "cmd": "status",
  "ok": true,
  "data": { ... },
  "error": null,
  "code": 0,
  "hint": null,
  "doc": null
}
.fi
失败时 \fIok=false / data=null / error/code/hint\fR 填充，envelope 仍写 stdout；
人类可读错误同步写 stderr。
.SH EXIT CODES
.TP
.B 0
成功
.TP
.B 1
通用失败（旧路径兼容）
.TP
.B 2
用法错（未识别命令 / 参数错）
.TP
.B 3
未找到资源（如 daemon 未声明）
.TP
.B 4
权限不足（sudo / 写文件失败）
.TP
.B 5
引擎未运行
.TP
.B 6
配置文件语法 / 字段错
.TP
.B 7
上游 API / 网络错
.TP
.B 8
并发锁未拿到（另一个 proxyctl 写操作正在跑）
.TP
.B 130
SIGINT（Ctrl-C）
.SH ENVIRONMENT
.TP
.B PROXYCTL_AGENT=1
一键 Agent 模式：等价 \fB--json\fR + \fB--no-color\fR + 非交互。
.TP
.B PROXYCTL_NO_COLOR=1
关闭 ANSI 颜色（同 \fB--no-color\fR）。
.TP
.B NO_COLOR
关闭 ANSI 颜色（https://no-color.org/）。
.TP
.B TERM=dumb
关闭 ANSI 颜色。
.SH FILES
.TP
.I ~/.config/proxyctl/config.yaml
proxyctl 自身配置（backend / api_secret / proxy_port / corp_dns / extra_daemons / no_proxy_extra / plugins_disabled）。
.TP
.I ~/.config/mihomo/config.yaml
mihomo 配置（rules / proxies / proxy-groups / proxy-providers / dns）。
.TP
.I ~/.config/sing-box/config.json
sing-box 配置。
.TP
.I ~/.config/proxyctl/plugins/*.py
用户插件目录。
.TP
.I ~/.config/proxyctl/.lock.*
写操作并发文件锁（fcntl.flock）。
.SH EXAMPLES
.PP
Agent 接入：
.PP
.RS 4
.nf
PROXYCTL_AGENT=1 proxyctl doctor      # JSON 健康分
proxyctl explain rules                # 想改规则去哪？
proxyctl commands --json | jq .data.commands[].name
proxyctl status --json | jq .data.engine.daemon_up
proxyctl bench --json proxy | jq -c 'select(.node)'
.fi
.RE
.PP
人类常用：
.PP
.RS 4
.nf
proxyctl status
proxyctl check
proxyctl trace github.com
proxyctl fix
proxyctl audit apply 7
.fi
.RE
.SH SEE ALSO
\fBmihomo\fR(1), \fBsing-box\fR(1), \fBlaunchctl\fR(1).
.PP
项目主页: \fIhttps://github.com/crhan/proxyctl\fR
.SH AUTHORS
crhan <crhan123@gmail.com>
